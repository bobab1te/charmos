# Free tier — get one at https://aistudio.google.com/apikey . Used for AI deal-parsing; leave blank to just use the manual form.
GEMINI_API_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Only needed for `npm run seed-demo` (Settings -> API in your Supabase project). Never exposed to the browser.
SUPABASE_SERVICE_ROLE_KEY=
